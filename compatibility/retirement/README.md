# Python Reference Retirement Evidence

The live CPython Agent Broker reference implementation was retired from this repository on 2026-08-16 after the full `make retirement_ready` gate passed.

Preserved evidence:
- language-neutral protocol/storage compatibility corpora under `compatibility/`,
- immutable golden fuzz seeds under `fuzz/seeds/`,
- SHA-256 identities of the deleted Python source/package/test/benchmark files in `python-reference-files.sha256`.

The deleted source contents are intentionally not archived here. Rust is the only live Agent Broker implementation and executable authority.
